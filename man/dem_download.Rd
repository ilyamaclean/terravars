% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{dem_download}
\alias{dem_download}
\title{Download Digital Elevation Data}
\source{
Terrain Tiles was accessed from
https://registry.opendata.aws/terrain-tiles.
}
\usage{
dem_download(
  r,
  mask = NULL,
  mask_value = NA,
  mask_out = c("NA", "0"),
  belowsea = FALSE
)
}
\arguments{
\item{r}{a \code{SpatRaster} object used as a template for downloading data,
with a coordinate reference system defined.}

\item{mask}{Optional \code{SpatRaster}, on the same grid as \code{r}, used to
mask cells out of the downloaded elevation data (see Details) --
this can be anything, e.g. a land-cover raster from
\code{\link[=landcover_download]{landcover_download()}} (to mask out water), or a mask of your own.
Not supplied (\code{NULL}, the default) leaves the raw downloaded
elevation data untouched.}

\item{mask_value}{Which value(s) in \code{mask} mark a cell to be masked
out. Default \code{NA}: cells where \code{mask} is \code{NA} are masked out. Set
to a specific value or vector of values if \code{mask} uses a
real-valued classification instead - e.g. \code{80} for sea when \code{mask}
is a \code{\link[=landcover_download]{landcover_download()}} raster with its default
\code{watersplit = TRUE} (its code \code{81}, freshwater, is then left
alone; pass \code{c(80, 81)} to mask out all water instead). Ignored if
\code{mask} isn't supplied. Follows the same convention as
\code{\link[=fillsinks]{fillsinks()}}'s own \code{waterbody}/\code{water_value} arguments.}

\item{mask_out}{Either \code{"NA"} (default) or \code{"0"}, controlling what
masked-out cells become in the returned raster: \code{NA}, or \code{0} (sea
level). Ignored if \code{mask} isn't supplied.}

\item{belowsea}{optional logical (default \code{FALSE}). When \code{FALSE}, any
cell in the returned raster still below 0 (sea level) is clamped to
\code{0} -- see Details. Set to \code{TRUE} to keep genuine below-sea-level
land (e.g. a polder) unmodified.}
}
\value{
a \code{SpatRaster} of digital elevation data, on the same grid
(resolution, extent, coordinate reference system) as \code{r}.
}
\description{
Downloads Digital Elevation Data directly from the Amazon
Web Services (AWS) public "Terrain Tiles" dataset - no account, API
key, or additional elevation-data package required.
}
\details{
The data returned matches the resolution, extent and
coordinate reference system of \code{r}. The resolution of the underlying
source tiles varies globally, so in some instances the data returned
after resampling to match \code{r} may be derived by interpolation rather
than reflecting the tiles' own native resolution. A map showing the
resolution of available data is at
https://github.com/tilezen/joerd/blob/master/docs/images/footprints-preview.png.

The elevation data available from AWS blends digital elevation and
bathymetry data, the latter generally only available at coarser
resolution, which frequently leaves coastal land cells with
spurious below-sea-level (negative) values instead of the small
positive elevations they should have. Two related fixes are applied
for this. First, whenever \code{mask} is supplied, any cell that is not
itself masked out but is immediately adjacent (8-connectivity) to a
cell that is masked out -- i.e. a land cell right at the coast --
is clamped to \code{0} if it comes out negative. This runs automatically
whenever \code{mask} is given, regardless of \code{belowsea}, since a
negative value immediately next to the sea is essentially always
this artefact rather than real below-sea-level terrain. Second,
\code{belowsea} controls a blanket version of the same fix applied to
the \emph{whole} returned raster rather than just cells next to \code{mask}:
by default (\code{belowsea = FALSE}), every remaining cell below \code{0} is
also clamped to \code{0}. Set \code{belowsea = TRUE} to skip this blanket
step and keep genuinely below-sea-level land elsewhere in \code{r}
(a polder, for example) at its true elevation -- the coastal fix
above still runs in that case if \code{mask} is supplied.

Data is fetched directly from the public, unauthenticated
\code{elevation-tiles-prod} Amazon S3 bucket (part of the AWS Registry of
Open Data - see https://registry.opendata.aws/terrain-tiles/) as
GeoTIFF tiles (the same source and format the \code{elevatr} package's
own \code{get_aws_terrain()} uses) and mosaicked internally. The zoom
level used is whichever single level guarantees resolution at
least as fine as \code{r}'s own resolution everywhere within \code{r}'s extent
(capped at zoom 14, the finest zoom at which the source tiles add
new data) - this can mean more resolution than strictly necessary is
fetched for an \code{r} spanning a wide range of latitudes, trading some
extra download size for a simpler, single-zoom mosaic.

Supplying \code{mask} (e.g. a \code{\link[=landcover_download]{landcover_download()}} raster, at 10 m
resolution - far finer than a generalised coastline vector, with
\code{mask_value = 80} to mask out its "Permanent water bodies" class)
also lets sea cells be masked out entirely instead of relying on
the elevation data's own land/sea classification. This function does
not fetch \code{mask} itself; call \code{\link[=landcover_download]{landcover_download()}} (or build
your own mask) and pass its result in explicitly, so the same
fetch can be reused across multiple \code{dem_download()} calls rather
than repeated on every one.
}
\examples{
\dontrun{
library(terra)
r <- rast(dtm100m)
dtm <- dem_download(r)
plot(dtm)

# Mask out water using a land-cover raster fetched separately, so it
# can be reused across more than one dem_download() call
lc <- landcover_download(r)
dtm_clean <- dem_download(r, mask = lc, mask_value = 80)

# Same, but sea level (0) instead of NA
dtm_clean0 <- dem_download(r, mask = lc, mask_value = 80, mask_out = "0")

# Keep genuine below-sea-level land away from the coast unmodified
dtm_polder <- dem_download(r, mask = lc, mask_value = 80, belowsea = TRUE)
}
}
