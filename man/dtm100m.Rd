% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{dtm100m}
\alias{dtm100m}
\title{Digital terrain model (Lizard, Cornwall)}
\format{
A wrapped \code{SpatRaster} (\code{terra::PackedSpatRaster}), single
layer, 100 m resolution, projected CRS (transverse Mercator, airy
ellipsoid - OSGB36 / British National Grid). Unwrap with
\code{terra::rast(dtm100m)} before use.
}
\source{
Elevation data for the Lizard peninsula, Cornwall, UK,
resampled to this package's own 100 m example grid.
}
\usage{
dtm100m
}
\description{
Example digital terrain model (DTM, elevation in metres) raster for the
Lizard peninsula, Cornwall, UK - the primary example dataset used
throughout this package's documentation and examples (every function
that takes a \code{dtm} argument uses this raster in its \verb{@examples}).
Defines the grid (resolution, extent, CRS) that every other bundled
dataset (\link{pai}, \link{hgt}, \link{vegx}, \link{landcover}) shares. \code{NA} cells (e.g.
open sea beyond the coastline) follow this package's usual land/sea
convention, used directly by e.g. \code{\link[=fillsinks]{fillsinks()}} and \code{\link[=coastalexposure]{coastalexposure()}}.
}
\examples{
\dontrun{
library(terra)
dtm <- rast(dtm100m)
plot(dtm)
}
}
\seealso{
\link{pai}, \link{hgt}, \link{vegx}, \link{landcover}, \link{dtmc}
}
\keyword{datasets}
