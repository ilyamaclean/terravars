% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{dtmc}
\alias{dtmc}
\title{Coarser regional elevation, for extending coastal searches (Lizard, Cornwall region)}
\format{
A list of two wrapped \code{SpatRaster} objects (\code{terra::PackedSpatRaster}), named \code{dtm1} and
\code{dtm2} (see Details for each one's resolution and extent). Unwrap each with \code{terra::rast()}
before use.
}
\source{
Elevation data via the \code{microclimdata} package's \code{dem_download()} function, which uses
the \code{elevatr} package to download global elevation data from AWS-hosted terrain tiles, matched
to the extent and resolution of whatever template raster it is given; land/sea mask via Natural
Earth (1:10m physical vectors, "land" category,
\url{https://www.naturalearthdata.com}).
}
\usage{
dtmc
}
\description{
A list of two coarser, larger-extent elevation rasters surrounding \code{dtm100m}'s own area, for use
with the \code{coarse} argument of \code{\link[=coastalexposure]{coastalexposure()}} -- lets a search based on \code{dtm100m}'s fine 100 m
grid reach much further out to sea (or further inland) than \code{dtm100m}'s own extent would otherwise
allow, without needing one enormous fine-resolution raster. \code{NA} cells represent sea and any
non-\code{NA} value (elevation, metres) represents land, matching \code{\link[=coastalexposure]{coastalexposure()}}'s
\code{landsea}/\code{coarse} convention.
}
\details{
\describe{
\item{\code{dtm1}}{500 m resolution, covering \code{dtm100m}'s own extent buffered outward by 50 km on
every side.}
\item{\code{dtm2}}{2500 m resolution, covering \code{dtm1}'s extent buffered outward by a further 500 km
on every side.}
}

Built by downloading elevation data onto two successively coarser, larger template grids, each
produced by aggregating the previous level five-fold (100 m -> 500 m -> 2500 m) and then extending
its extent outward, so each level's resolution and extent were fixed by its own template grid
before any data was fetched for it. Land/sea masking was applied afterward using Natural Earth's
1:10m physical land polygons, reprojected to \code{dtm100m}'s own CRS; both layers' CRS was then
explicitly set to match \code{dtm100m} exactly, rather than relying on whatever CRS the reprojection
step itself returned, avoiding spurious CRS-mismatch issues downstream even though the projection
is unchanged either way.
}
\examples{
\dontrun{
library(terra)
plot(rast(dtmc$dtm1))

# Pass the whole list straight in as 'coarse' to extend a
# coastalexposure() search well beyond dtm100m's own extent -- its
# elements are wrapped (terra::PackedSpatRaster) and get unwrapped
# automatically, no need to call rast() on them first
landsea <- rast(dtm100m)
ce <- coastalexposure(landsea, wdir = 270, coarse = dtmc)
}
}
\seealso{
\link{landcover}, \code{\link[=coastalexposure]{coastalexposure()}}
}
\keyword{datasets}
