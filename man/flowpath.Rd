% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wetness.R
\name{flowpath}
\alias{flowpath}
\title{Trace a flow path}
\usage{
flowpath(
  dtm,
  xy,
  method = c("d8", "d4"),
  route = c("steepest", "mfd"),
  out = c("downstream", "upstream"),
  flat_source = TRUE,
  waterbody = NULL,
  water_value = NA
)
}
\arguments{
\item{dtm}{A \code{SpatRaster} of elevation values (metres).}

\item{xy}{Numeric vector of length 2, \code{c(x, y)}, the coordinates of the
point in \code{dtm}'s own coordinate reference system. Must fall within
\code{dtm}'s extent, on a non-\code{NA} cell.}

\item{method}{Character. \code{"d8"} (default): the path/spread may move to
any of the 8 surrounding cells. \code{"d4"}: only the 4 orthogonal
(N/S/E/W) neighbours are considered.}

\item{route}{Character. \code{"steepest"} (default): a single representative
line, one step at a time. \code{"mfd"}: spread across every valid
neighbour at once, weighted by slope - see Details.}

\item{out}{Character. \code{"downstream"} (default): trace where the
point's own water goes. \code{"upstream"}: trace where the water arriving
at the point could have come from - see Details.}

\item{flat_source}{Logical. \code{TRUE} (default): also treat any
same-elevation cell immediately adjacent to \code{xy} as part of the
source (a rough flat-water-body approximation - see Details).
\code{FALSE}: \code{xy}'s own cell only. Ignored whenever \code{waterbody} is
supplied (the water body it identifies is used instead).}

\item{waterbody}{Optional \code{SpatRaster}, on the same grid as \code{dtm},
identifying real water bodies - see Details. \code{NULL} (default): no
water body raster; \code{flat_source} (or \code{xy}'s own cell alone) is used
instead.}

\item{water_value}{Which value(s) in \code{waterbody} mark a cell as water.
Default \code{NA}: cells where \code{waterbody} is \code{NA} count as water,
matching \code{\link[=fillsinks]{fillsinks()}}'s own \code{waterbody}/\code{water_value} convention.
Supply one or more explicit values instead if \code{waterbody} uses a
different convention. Ignored if \code{waterbody} isn't supplied.}
}
\value{
A \code{SpatRaster} on the same grid as \code{dtm}, named
\verb{flowpath_<out>_<route>_<method>}; see "Value scheme" above for what
the cell values themselves mean. Masked to \code{dtm}'s own \code{NA} pattern
(\code{NA} wherever \code{dtm} is \code{NA}).
}
\description{
Follows water step by step from a single point - either downhill
(\code{out = "downstream"}, default: where the point's own water goes) or
uphill (\code{out = "upstream"}: where the water arriving at the point could
have come from) - always moving to whichever neighbouring cell is the
steepest way down (or up), until it reaches a pit or ridge, the edge of
\code{dtm}, or an \code{NA} cell. \code{route} offers a second option in either
direction: instead of a single representative line, water is
apportioned across every valid neighbour at once, weighted by slope,
giving a continuous layer rather than one traced line. Complements
\code{\link[=flowacc]{flowacc()}}, which looks upslope in aggregate (how much area drains
\emph{through} every cell of a whole DTM) by instead working outward from
one fixed point, in either direction.
}
\details{
\strong{Neighbourhood} (\code{method}): at every step, the next cell is whichever
neighbour - \code{"d8"} (default): any of the 8 surrounding cells; \code{"d4"}:
only the 4 orthogonal neighbours - is the steepest way down (or up, for
\code{out = "upstream"}) from the current cell. This is the same
steepest-descent rule \code{\link[=flowacc]{flowacc()}}'s own \code{method = "d8"} routes with: a
diagonal neighbour's elevation drop (or rise) is divided by
\eqn{\sqrt{2}} before comparing to an orthogonal neighbour's, since it
sits farther away, so it only wins if it's genuinely steeper, not just
higher or lower.

\strong{Routing} (\code{route}): \code{"steepest"} (default) follows a single line,
one step at a time, ending as soon as no neighbour is strictly lower
(or, for \code{out = "upstream"}, strictly higher - a ridge or local peak)
than the current cell, the search reaches the edge of \code{dtm}, or it
reaches an \code{NA} cell. Run \code{\link[=fillsinks]{fillsinks()}} first if you want a downstream
trace to continue through small artefact depressions rather than
stopping dead at every one. \code{"mfd"} instead spreads the water across
every valid neighbour at once, in proportion to
\eqn{\text{slope}^{1.1}} - the same weighting \code{\link[=flowacc]{flowacc()}}'s own
\code{method = "mfd"} uses - rather than committing to a single line: for
\code{out = "downstream"} this gives a diffusion-like layer showing how much
of the point's water reaches each downstream cell; for
\code{out = "upstream"} it instead gives the full upslope contributing area,
graded by how much of \emph{each individual upslope cell's} water actually
reaches the point, rather than a flat yes/no catchment boundary - e.g.
a pollutant turns up at a monitoring point and you want to know how
plausible each part of the upslope area is as the source.

\strong{Value scheme:} \code{route = "steepest"} returns \code{2} for the point itself
(see \code{flat_source}/\code{waterbody} below - more than one cell can count as
"the point"), \code{1} for every other traced cell, \code{0} elsewhere.
\code{route = "mfd"} instead returns \code{1} for the point itself and a value
between 0 and 1 everywhere else (the diffused/contributing fraction) -
\code{2} would sit oddly on an otherwise continuous 0-1 scale, so the point
is reported as \code{1} (all of the water) instead.

\strong{Treating the point as a small water body} (\code{flat_source}): by
default (\code{TRUE}), \code{xy} is treated as sitting at the centre of a small,
flat water body rather than a single infinitely small point: any cell
immediately adjacent to it (again, using \code{method}'s neighbourhood - 8
or 4 cells) that shares its exact elevation is treated the same way -
included in the source, and its own path/contribution traced too. Set
\code{flat_source = FALSE} to use \code{xy}'s own cell alone instead. Multiple
source paths/diffusions can converge onto the same route; for
\code{route = "steepest"}, once one reaches a cell already reached by
another, tracing stops there rather than retracing the rest of the
route.

\strong{A real water body instead} (\code{waterbody}, \code{water_value}): a rougher
but more principled alternative to \code{flat_source}'s same-elevation
guess - supply an actual land-cover (or other) raster identifying real
water bodies, on the same grid as \code{dtm}, and the \emph{whole} connected
water body (8-connectivity) containing \code{xy} is used as the source
instead of \code{flat_source}'s same-elevation neighbours (which is then
ignored, whatever it's set to - there's no reason to supply
\code{waterbody} otherwise). Follows the same convention as
\code{\link[=fillsinks]{fillsinks()}}'s own \code{waterbody}/\code{water_value}: \code{water_value = NA}
(default) treats \code{NA} cells in \code{waterbody} as water; supply one or
more explicit value(s) instead if \code{waterbody} marks water with a real
code. A warning is issued, and a single-cell source used instead, if
\code{xy} doesn't actually fall on a \code{waterbody} cell.
}
\examples{
\dontrun{
library(terra)
dtm <- rast(dtm100m)
dtm_filled <- fillsinks(dtm)

# Where does the water at this point go?
fp <- flowpath(dtm_filled, xy = c(170000, 12000))
plot(fp)

# Same, but spread across every downhill neighbour rather than a single line
fp_mfd <- flowpath(dtm_filled, xy = c(170000, 12000), route = "mfd")
plot(fp_mfd)

# Where could the water at this point have come from? e.g. tracing a
# pollutant detected at a monitoring point back to its plausible source area
fp_up <- flowpath(dtm_filled, xy = c(170000, 12000), route = "mfd", out = "upstream")
plot(fp_up)

# Restrict to orthogonal (rook's-case) neighbours only
fp4 <- flowpath(dtm_filled, xy = c(170000, 12000), method = "d4")

# Use an actual water body mask instead of the same-elevation guess
lc <- rast(landcover)   # bundled land cover data (see ?landcover)
fp_wb <- flowpath(dtm_filled, xy = c(170000, 12000), waterbody = lc,
                   water_value = c(13, 14))   # 13 = Saltwater, 14 = Freshwater
}
}
\seealso{
\code{\link[=flowacc]{flowacc()}}, \code{\link[=fillsinks]{fillsinks()}}, \code{\link[=twi]{twi()}}
}
