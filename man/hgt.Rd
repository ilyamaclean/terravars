% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{hgt}
\alias{hgt}
\title{Canopy height (Lizard, Cornwall)}
\format{
A wrapped \code{SpatRaster} (\code{terra::PackedSpatRaster}), single
layer, 100 m resolution. Unwrap with \code{terra::rast(hgt)} before use.
}
\source{
Lang, N., Jetz, W., Schindler, K., & Wegner, J.D. (2023). A
high-resolution canopy height model of the Earth. \emph{Nature Ecology &
Evolution}, 1--12.
}
\usage{
hgt
}
\description{
Example canopy height (metres above local ground) raster for the Lizard
peninsula, Cornwall, UK - how tall the vegetation is at each point. For
use with the \code{hgt} argument of \code{\link[=solarindex]{solarindex()}}, \code{\link[=skyview]{skyview()}} and
\code{\link[=canopy3d]{canopy3d()}}, which switches those functions on to a more realistic
calculation that accounts for the actual 3D shape of nearby vegetation
rather than assuming it forms a uniform layer. Same grid (resolution,
extent, CRS) as \code{dtm100m}.
}
\details{
Derived from the global canopy height model of Lang et al. (2023).
}
\examples{
\dontrun{
library(terra)
hgt_r <- rast(hgt)
plot(hgt_r)
}
}
\seealso{
\link{pai}, \link{vegx}, \link{landcover}, \code{\link[=solarindex]{solarindex()}}, \code{\link[=skyview]{skyview()}}, \code{\link[=canopy3d]{canopy3d()}}
}
\keyword{datasets}
