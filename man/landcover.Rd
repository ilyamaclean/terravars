% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{landcover}
\alias{landcover}
\title{Habitat / land cover classification (Lizard, Cornwall)}
\format{
A wrapped \code{SpatRaster} (\code{terra::PackedSpatRaster}), single
layer, 100 m resolution, integer class codes 1--21 (see Details for
the full lookup table). Unwrap with \code{terra::rast(landcover)} before use.
}
\source{
European Space Agency 10 m resolution land cover, resampled
(modal) to \code{dtm100m}'s grid.
}
\usage{
landcover
}
\description{
Example habitat/land cover raster for the Lizard peninsula, Cornwall,
UK, using the 21-class habitat scheme in the table below. This is the
land cover data \link{vegx}'s leaf angle distribution lookup table was built
from. Same grid (resolution, extent, CRS) as \code{dtm100m}. Classes 13
(Saltwater) and 14 (Freshwater) make this a ready-made example for
\code{\link[=fillsinks]{fillsinks()}}'s \code{waterbody} argument (e.g. \code{water_value = c(13, 14)}).
}
\details{
Source data is the European Space Agency's 10 m resolution land cover
product, resampled to \code{dtm100m}'s 100 m grid using modal ("mode")
resampling -- the appropriate choice for categorical data, since it
keeps whichever class is most common within each 100 m cell rather than
blending neighbouring classes together the way a numeric resampling
method would -- reprojected/aligned to \code{dtm100m}'s own CRS and extent,
and masked to match it (\code{NA} wherever \code{dtm100m} is \code{NA}, e.g. open sea
beyond the coastline, this package's usual convention).\tabular{rl}{
   Code \tab Habitat \cr
   1 \tab Broadleaved woodland \cr
   2 \tab Coniferous woodland \cr
   3 \tab Arable and horticulture \cr
   4 \tab Improved grassland \cr
   5 \tab Neutral grassland \cr
   6 \tab Calcareous grassland \cr
   7 \tab Acid grassland \cr
   8 \tab Fen, marsh and swamp \cr
   9 \tab Heather \cr
   10 \tab Heather grassland \cr
   11 \tab Bog \cr
   12 \tab Inland rock \cr
   13 \tab Saltwater \cr
   14 \tab Freshwater \cr
   15 \tab Supralittoral rock \cr
   16 \tab Supralittoral sediment \cr
   17 \tab Littoral rock \cr
   18 \tab Littoral sediment \cr
   19 \tab Saltmarsh \cr
   20 \tab Urban \cr
   21 \tab Suburban \cr
}
}
\examples{
\dontrun{
library(terra)
landcover_r <- rast(landcover)
plot(landcover_r)

# Use as a water body mask for fillsinks() -- Saltwater and Freshwater
# are classes 13 and 14
dtm <- rast(dtm100m)
dtm_filled <- fillsinks(dtm, waterbody = landcover_r, water_value = c(13, 14))
}
}
\seealso{
\link{pai}, \link{hgt}, \link{vegx}, \code{\link[=fillsinks]{fillsinks()}}
}
\keyword{datasets}
