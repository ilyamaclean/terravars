% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{landcover_download}
\alias{landcover_download}
\title{Download ESA WorldCover Land Cover Data}
\source{
ESA WorldCover 10 m 2020 v100, Zenodo
https://doi.org/10.5281/zenodo.5571936; ESA WorldCover 10 m 2021
v200, Zenodo https://doi.org/10.5281/zenodo.7254221.
}
\usage{
landcover_download(r, year = 2021, watersplit = TRUE)
}
\arguments{
\item{r}{a \code{SpatRaster} object used as a template for downloading data,
with a coordinate reference system defined.}

\item{year}{optional numeric, either \code{2020} or \code{2021} (default),
selecting which WorldCover release to fetch (\code{2020} = v100, \code{2021}
= v200 - the more recent release).}

\item{watersplit}{optional logical (default \code{TRUE}) splitting code
\code{80} ("Permanent water bodies") into two: \code{80} (sea) and \code{81}
(freshwater) - see Details.}
}
\value{
a \code{SpatRaster} of land-cover class codes, on the same grid
(resolution, extent, coordinate reference system) as \code{r}.
}
\description{
Downloads ESA WorldCover 10 m resolution global land-cover
data directly from its public Amazon Web Services (AWS) home - no
account, API key, or additional package required.
}
\details{
The data returned matches the resolution, extent and
coordinate reference system of \code{r}. Because land-cover class codes
are categorical, resampling onto \code{r}'s grid uses modal ("mode")
resampling - whichever class is most common within each output
cell - rather than the interpolation used for continuous data like
elevation.

Data is fetched directly from the public, unauthenticated
\code{esa-worldcover} Amazon S3 bucket (part of the AWS Registry of Open
Data - see https://registry.opendata.aws/esa-worldcover-vito/) as
3x3 degree GeoTIFF tiles and mosaicked internally.

Returned values follow the ESA WorldCover 10 m classification:\tabular{llll}{
   Code \tab Class \tab Code \tab Class \cr
   10 \tab Tree cover \tab 70 \tab Snow and ice \cr
   20 \tab Shrubland \tab 80 \tab Permanent water bodies (sea) \cr
   30 \tab Grassland \tab 81 \tab Permanent water bodies (freshwater) \cr
   40 \tab Cropland \tab 90 \tab Herbaceous wetland \cr
   50 \tab Built-up \tab 95 \tab Mangroves \cr
   60 \tab Bare / sparse vegetation \tab 100 \tab Moss and lichen \cr
}


ESA WorldCover's own code \code{80} doesn't distinguish sea from a large
inland lake or river - both are "Permanent water bodies", per its
Product User Manual ("lakes, reservoirs, and rivers... either
fresh or salt-water bodies"). When \code{watersplit = TRUE} (default),
code \code{80} is split into two after fetching: connected water
patches (8-connectivity) that touch \code{r}'s own edge are presumed
connected to open water beyond this extent and kept as \code{80} (sea);
patches fully enclosed within the interior, never touching the
edge, become \code{81} (freshwater) - a new code, not part of ESA's own
scheme, chosen from the unused gap between \code{80} and \code{90} so it
can't collide with a real WorldCover class. This is a heuristic
based purely on whether a water body touches \code{r}'s edge: a lake
that happens to sit right at the edge of \code{r}'s extent (but isn't
actually the sea) would be called \code{80}, and a tidal inlet fully
enclosed within a large \code{r} would be called \code{81}. Set
\code{watersplit = FALSE} to keep ESA's original undivided code \code{80}
for all water.

The result is a natural fit for \code{\link[=dem_download]{dem_download()}}'s \code{mask} argument
(e.g. \code{mask_value = 80} to mask out sea only, leaving inland water
bodies alone) - a 10 m land-cover classification distinguishes
land from water at far finer detail than a generalised global
coastline vector.
}
\examples{
\dontrun{
library(terra)
r <- rast(dtm100m)
lc <- landcover_download(r)
plot(lc)

# Keep ESA's original undivided water code
lc_raw <- landcover_download(r, watersplit = FALSE)
}
}
