% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{pai}
\alias{pai}
\title{Plant area index (Lizard, Cornwall)}
\format{
A wrapped \code{SpatRaster} (\code{terra::PackedSpatRaster}), single
layer, 100 m resolution. Unwrap with \code{terra::rast(pai)} before use.
}
\source{
Leaf area index derived from Sentinel data via WEkEO
(\url{https://www.wekeo.eu}).
}
\usage{
pai
}
\description{
Example plant area index (PAI, m^2 m^-2) raster - a measure of how much
leaf, stem and branch material sits above each point in the landscape,
higher values meaning denser vegetation - for the Lizard peninsula,
Cornwall, UK. For use with the \code{pai} argument of \code{\link[=solarindex]{solarindex()}},
\code{\link[=skyview]{skyview()}}, \code{\link[=twostream]{twostream()}} and \code{\link[=canopy3d]{canopy3d()}}. Same grid (resolution,
extent, CRS) as \code{dtm100m}.
}
\details{
Derived from Sentinel-based leaf area index (LAI), obtained via WEkEO
(the Copernicus Data and Information Access Service); used here as an
approximation of plant area index (PAI includes non-leaf plant material
such as stems and branches, which LAI alone does not capture).
}
\examples{
\dontrun{
library(terra)
pai_r <- rast(pai)
plot(pai_r)
}
}
\seealso{
\link{hgt}, \link{vegx}, \link{landcover}, \code{\link[=solarindex]{solarindex()}}, \code{\link[=skyview]{skyview()}}, \code{\link[=twostream]{twostream()}}, \code{\link[=canopy3d]{canopy3d()}}
}
\keyword{datasets}
