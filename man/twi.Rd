% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wetness.R
\name{twi}
\alias{twi}
\title{Topographic wetness index}
\usage{
twi(
  dtm,
  method = "modified",
  flow_acc = NULL,
  route = NULL,
  weight = NULL,
  bsn = NULL,
  slope = NULL,
  min_slope = 0.1,
  fillna = FALSE
)
}
\arguments{
\item{dtm}{A \code{SpatRaster} of elevation values (metres) - your terrain
model.}

\item{method}{Character. Which TWI formula to use: \code{"standard"},
\code{"modified"} (default), or \code{"saga"} - see Details.}

\item{flow_acc}{Optional \code{SpatRaster} of flow accumulation, used as a
stand-in for the contributing area \code{a} above -- typically the output
of \code{\link[=flowacc]{flowacc()}}. Expected to be \emph{self-inclusive} (every cell's own
weight already counted, so a headwater cell reads its own weight
rather than 0), matching \code{\link[=flowacc]{flowacc()}}'s own convention -- if you
import a flow accumulation layer computed elsewhere (ArcGIS,
whitebox, etc.) that instead follows the older self-excluding
convention (headwater cells read 0), this is detected automatically
(a minimum value of exactly 0 anywhere in the raster) and corrected
for internally, so either convention works without you needing to
know which one your own raster follows. If \code{NULL}, worked out from
\code{dtm} automatically via \code{\link[=flowacc]{flowacc()}} -- see \code{route}/\code{weight}/\code{bsn}
below.}

\item{route}{Optional character, one of \code{"d8"}, \code{"mfd"}, \code{"dinf"} --
which \code{\link[=flowacc]{flowacc()}} routing method to use when \code{flow_acc} isn't
supplied. Left at \code{NULL} (default), auto-selects \code{"mfd"} for
\code{method = "saga"} and \code{"d8"} otherwise -- see Details. Ignored if
\code{flow_acc} is supplied directly.}

\item{weight}{Optional \code{SpatRaster} of per-cell weights, passed
straight through to the internal \code{\link[=flowacc]{flowacc()}} call (see its own
\code{weight} argument). Ignored if \code{flow_acc} is supplied directly.}

\item{bsn}{Optional \code{SpatRaster} of basin IDs, passed straight through
to the internal \code{\link[=flowacc]{flowacc()}} call to restrict accumulation to stay
within each basin (see its own \code{bsn} argument). Ignored if
\code{flow_acc} is supplied directly.}

\item{slope}{Optional \code{SpatRaster} of slope (degrees). If \code{NULL}, worked
out from \code{dtm} automatically.}

\item{min_slope}{Numeric. The minimum slope (degrees) allowed before
dividing by \code{tan(slope)}, used by the \code{"modified"} and \code{"saga"}
methods to avoid producing infinite values on flat ground. Default is
\code{0.1}.}

\item{fillna}{Logical. If \code{TRUE}, \code{NA} cells in \code{dtm} (e.g. sea, or any
other no-data gap), and the outer ring of \code{dtm} itself, are
temporarily filled from their nearest real neighbour before slope is
worked out, and the result is masked back to \code{dtm}'s own \code{NA} pattern
afterwards - avoids slope (and the TWI itself) coming out \code{NA} at
land cells that merely touch an \code{NA} cell, e.g. an entire coastline's
worth of cells picking up \code{NA} purely for being next to the sea, and
at the raster's own edge, where the same problem occurs because
there's no neighbouring cell at all just past the boundary. Default
\code{FALSE}. See \code{\link[=solarindex]{solarindex()}}'s own \code{fillna} for the full rationale.}
}
\value{
A \code{SpatRaster} of TWI values.
}
\description{
Estimates how likely each point in the landscape is to be wet, based
purely on its position in the terrain: flatter spots that collect
drainage from a large area upslope score higher (wetter), while steep
ground or high points with little upslope area draining into them score
lower (drier). This is a simple, terrain-only proxy for soil moisture -
it doesn't know anything about rainfall, soil type, or vegetation - but
it's widely used as a quick first look at where water is likely to
pool or drain away. Three closely related formulations are supported to
match different conventions used in the literature.
}
\details{
The standard TWI (Beven & Kirkby 1979) is:
\deqn{TWI = \ln\!\left(\frac{a}{\tan\beta}\right)}
where \eqn{a} is the specific catchment area (the upslope area draining
through each point, per unit of contour length - in effect, "how much
water is likely funnelling through here") and \eqn{\beta} is the local
slope steepness. A larger contributing area or a flatter slope both push
the index up (wetter); a steeper slope pushes it down (drier).

\strong{Methods} (the TWI formula itself -- this is a completely separate
choice from how the underlying flow accumulation is \emph{routed}, see
\code{route} below):
\itemize{
\item \code{"standard"}: the classical formulation above; completely flat cells
(slope = 0) produce \code{Inf}, since the formula divides by \code{tan(slope)}.
\item \code{"modified"} (default): avoids that \code{Inf} problem by placing a floor
under how flat a slope is allowed to be (\code{min_slope}) before doing the
division (Gruber & Peckham 2009).
\item \code{"saga"}: the SAGA GIS wetness index (Boehner & Selige 2006), a variant
that estimates the contributing area a little differently and also
applies the same slope floor. Its contributing-area term is also
capped internally at \code{res * 1e6} (an arbitrarily large but finite
ceiling, in the same units as the specific catchment area itself) so
that an extreme, unbounded accumulation value can't dominate the
result; this only matters for catchments of an unusually large scale.
}

The upslope contributing area is worked out internally via
\code{\link[=flowacc]{flowacc()}} unless you supply your own already-computed accumulation
via \code{flow_acc}. \code{route} picks which of \code{\link[=flowacc]{flowacc()}}'s routing methods to
use for that internal call; left at its default (\code{NULL}), it
auto-selects \code{"mfd"} when \code{method = "saga"} (matching SAGA's own
literature-typical multiple-flow-direction routing, rather than a
plain single-direction D8 path) and \code{"d8"} for \code{"standard"}/\code{"modified"}.
Set \code{route} explicitly to override this for any \code{method}. \code{weight} and
\code{bsn} are passed straight through to that internal \code{\link[=flowacc]{flowacc()}} call
(same meaning as there: a per-cell weight raster, and/or restricting
accumulation to stay within each basin of a supplied basin-ID raster).
All three (\code{route}/\code{weight}/\code{bsn}) are simply ignored if \code{flow_acc} is
supplied directly, since in that case every routing decision has
already been made by whoever computed it.
}
\examples{
\dontrun{
library(terra)
dtm <- rast(dtm100m)
dtm_filled <- fillsinks(dtm)
w   <- twi(dtm_filled, method = "modified")
plot(w)

# saga formula, auto-routed via mfd
w_saga <- twi(dtm_filled, method = "saga")

# force D-infinity routing regardless of method
w_dinf <- twi(dtm_filled, method = "modified", route = "dinf")

# DTM with NA cells at a coastline: avoid an NA halo on land
w_coast <- twi(dtm_filled, method = "modified", fillna = TRUE)
}
}
\references{
Beven, K.J. & Kirkby, M.J. (1979). A physically based, variable contributing
area model of basin hydrology. \emph{Hydrological Sciences Bulletin}, 24, 43-69.

Gruber, S. & Peckham, S. (2009). Land-surface parameters and objects in
hydrology. \emph{Developments in Soil Science}, 33, 171-194.

Boehner, J. & Selige, T. (2006). Spatial prediction of soil attributes using
terrain analysis and climate regionalisation. \emph{SAGA - Analysis and Modelling
Applications}, 115, 13-28.
}
\seealso{
\code{\link[=flowacc]{flowacc()}}, \code{\link[=fillsinks]{fillsinks()}}, \code{\link[=flowpath]{flowpath()}}
}
