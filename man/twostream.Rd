% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solar.R
\name{twostream}
\alias{twostream}
\title{Two-stream canopy radiation model}
\usage{
twostream(
  dtm,
  pai,
  tme,
  x = NULL,
  lref = NULL,
  ltra = NULL,
  gref = 0.15,
  output = "mean",
  pai_horiz = TRUE,
  fillna = FALSE
)
}
\arguments{
\item{dtm}{A \code{SpatRaster} of elevation values (metres) - your terrain
model. Must use a projected CRS with metre units. Slope and aspect are
worked out from it automatically.}

\item{pai}{A \code{SpatRaster} of plant area index (m^2 m^-2) - a measure of
how much leaf, stem and branch material sits above each point (higher
values mean a denser canopy that blocks more light).}

\item{tme}{A \code{POSIXct} or \code{POSIXlt} vector of times \strong{in UTC}. Unlike
some other functions in this package, the sun's position here is
always worked out separately for every single grid cell from its own
geographic coordinates (more accurate over large maps, but slower).}

\item{x}{Leaf angle distribution parameter. Controls how the canopy's
leaves are typically angled, which affects how much light passing
through gets blocked versus slipping through gaps: leave it at the
default (1) for a "typical" mixed canopy if unsure, use a value close
to 0 for canopies of mostly upright/vertical leaves (e.g. many
grasses and some conifers), or a large value (or \code{Inf}) for canopies
of mostly flat, horizontal leaves (e.g. many broadleaf trees).
(Technically, the ratio of leaf area projected onto a horizontal plane
versus a vertical one, following the Campbell & Norman (1998)
ellipsoidal convention.) May be a scalar, \code{SpatRaster}, or \code{NA}/\code{NULL}.}

\item{lref}{Leaf reflectance (0--1) - the fraction of light hitting a
leaf that bounces straight back off it. \code{NULL} or \code{NA} uses 0.4.
May be a scalar, \code{SpatRaster}, or \code{NA}/\code{NULL}.}

\item{ltra}{Leaf transmittance (0--1) - the fraction of light hitting a
leaf that passes straight through it. \code{NULL} or \code{NA} uses 0.2.
Note \code{lref + ltra} must be < 1 (a leaf can't reflect and transmit more
light than hits it). May be a scalar, \code{SpatRaster}, or \code{NA}/\code{NULL}.}

\item{gref}{Ground reflectance (0--1) - the fraction of light hitting
the bare ground beneath the canopy that bounces back upward (higher
for pale, dry, or snow-covered ground; lower for dark, wet soil).
Default 0.15. May be a scalar or \code{SpatRaster}.}

\item{output}{Character. \code{"mean"} (default) returns a single 3-layer
\code{SpatRaster} averaged across all the timesteps in \code{tme}. \code{"stack"}
instead returns a named list of three \code{SpatRaster} objects
(\code{fdirdown}, \code{fdifdown}, \code{fdirscat}), each with one layer per
timestep, so you can inspect individual timesteps rather than only
the average.}

\item{pai_horiz}{Logical. Declares which area the supplied \code{pai} is
measured against: \code{TRUE} (default) - per unit \strong{horizontal} ground
area (the usual leaf/plant area index convention); \code{FALSE} - per unit
\strong{sloped} ground-surface area. Identical on flat ground; on a slope
they differ by \code{cos(slope)}. Unlike \code{\link[=solarindex]{solarindex()}} (which is purely a
direct-beam calculation), the two streams here need \code{pai} in different
conventions - the direct beam is attenuated as a slope-parallel layer
(per sloped area) while the diffuse two-stream assumes a
horizontally-homogeneous layer (per horizontal area) - so each is fed
\code{pai} converted to its own convention internally. Set \code{pai_horiz} to
match how your \code{pai} data was derived; it changes the result on
sloping ground. See Details.}

\item{fillna}{Logical. If \code{TRUE}, \code{NA} cells in \code{dtm} (e.g. sea, or any
other no-data gap), and the outer ring of \code{dtm} itself, are
temporarily filled from their nearest real neighbour before
slope/aspect are worked out, and every output layer is masked back to
\code{dtm}'s own \code{NA} pattern afterwards - avoids slope/aspect (and
everything downstream of them) coming out \code{NA} at land cells that
merely touch an \code{NA} cell, e.g. an entire coastline's worth of cells
picking up \code{NA} purely for being next to the sea, and at the raster's
own edge, where the same problem occurs because there's no
neighbouring cell at all just past the boundary. Default \code{FALSE}.
See \code{\link[=solarindex]{solarindex()}}'s own \code{fillna} for the full rationale.}
}
\value{
A 3-layer \code{SpatRaster} (layers: \code{fdirdown}, \code{fdifdown},
\code{fdirscat}) when \code{output = "mean"}, or a named list of three
\code{SpatRaster} objects when \code{output = "stack"}.
}
\description{
Computes what fraction of the sunlight arriving above a canopy actually
makes it down to the ground beneath, worked out separately for the sun's
direct beam and for diffuse sky light (light scattered across the rest of
the sky), and accounting for the terrain's slope and how reflective the
leaves and ground are. This is a more physically detailed calculation
than simply assuming a fixed proportion of light is blocked: it also
tracks light that bounces between leaves and the ground one or more times
before it eventually reaches the ground or escapes back out, rather than
treating any light that touches a leaf as lost. (Technically, this is the
Sellers (1985) two-stream approximation, with the sun's angle relative to
the slope handled via the Campbell & Norman (1998) extinction-coefficient
formulation.)
}
\details{
Each output is a fraction between 0 and 1: multiply it by the
corresponding above-canopy sunlight amount (\code{swdir} for the sun's direct
beam, \code{swdif} for diffuse sky light) to get the actual amount reaching
the ground. Three fractions are returned per cell, describing three
different fates for the light:

\describe{
\item{\code{fdirdown}}{How much of the sun's direct beam reaches the ground
still as a direct beam, having passed through the canopy without
being intercepted. Depends on the sun's angle, the slope, and how
dense the canopy is (\code{pai}).}
\item{\code{fdifdown}}{How much of the diffuse sky light reaches the
ground. Doesn't depend on the sun's position at all - only on how
dense the canopy is and how reflective/absorbent the leaves are.}
\item{\code{fdirscat}}{How much of the sun's direct beam gets caught by
the canopy, bounced around, and eventually reaches the ground as
scattered, diffuse-like light rather than passing straight through.
This is the extra light that a simpler "canopy just dims light
passing through it" model would miss entirely.}
}

To get the total below-canopy sunlight, add all three together: total
downward = \code{swdir * fdirdown + swdif * fdifdown + swdir * fdirscat},
where \code{swdir} and \code{swdif} are the above-canopy direct and diffuse
sunlight amounts you're starting from.

\strong{Leaf reflectance and transmittance} (\code{lref}, \code{ltra}) default to
typical values for broadband sunlight (0.4 and 0.2). If you're working
with a specific part of the light spectrum instead (e.g.
photosynthetically active radiation, PAR), supply your own values as
rasters.

\strong{Plant area index convention} (\code{pai_horiz}): \code{pai} can be supplied per
unit horizontal ground area (\code{TRUE}, default - the usual convention) or
per unit sloped ground-surface area (\code{FALSE}), the two differing by
\code{cos(slope)} on sloping ground. The direct-beam fraction (\code{fdirdown})
treats the canopy as a slope-parallel layer and so needs \code{pai} per sloped
area, whereas the diffuse fraction (\code{fdifdown}) solves the two-stream
equations for a horizontally-homogeneous layer and so needs \code{pai} per
horizontal area; the scattered fraction (\code{fdirscat}) draws on both.
Whichever convention you supply, each fraction is computed from \code{pai}
converted to its own convention, so a fixed physical canopy attenuates
the beam identically either way and only the reported area basis differs.
On flat ground \code{pai_horiz} has no effect. Leaf angles are always
referenced to the horizontal and never rotate with the ground.

When you supply more than one time in \code{tme}, results are averaged across
all of them by default (\code{output = "mean"}). Night-time timesteps count
as zero toward the direct and scattered fractions, so supply only
daytime times if you want a daytime-only average. \code{fdifdown} doesn't
depend on the sun's position at all, so it comes out identical
regardless of which timesteps you include.
}
\examples{
\dontrun{
library(terra)
dtm  <- rast(dtm100m)
pai  <- rast(pai)          # plant area index (Lizard, Cornwall)
vegx <- rast(vegx)         # leaf angle distribution parameter

tme <- as.POSIXct("2023-06-21 10:00", tz = "UTC")

# Single instant, default leaf angle distribution (spherical)
ts <- twostream(dtm, pai, tme)
plot(ts)

# Single instant, with the real (spatially variable) leaf angle distribution
ts_x <- twostream(dtm, pai, tme, x = vegx)

# Mean over a day
tme_day <- seq(as.POSIXct("2023-06-21 05:00", tz = "UTC"),
               as.POSIXct("2023-06-21 20:00", tz = "UTC"),
               by = "1 hour")
ts_day <- twostream(dtm, pai, tme_day, x = vegx)

# Hourly stack with spatially variable leaf properties
lref <- rast(dtm); terra::values(lref) <- 0.35
ts_stack <- twostream(dtm, pai, tme_day, x = vegx, lref = lref, output = "stack")

# DTM with NA cells at a coastline: avoid an NA halo on land
ts_coast <- twostream(dtm, pai, tme, fillna = TRUE)
}
}
\references{
Sellers, P.J. (1985). Canopy reflectance, photosynthesis and
transpiration. \emph{International Journal of Remote Sensing}, 6,
1335--1372.

Campbell, G.S. & Norman, J.M. (1998). \emph{An Introduction to
Environmental Biophysics}, 2nd ed. Springer, New York.
}
\seealso{
\code{\link[=solarindex]{solarindex()}}, \code{\link[=horizon]{horizon()}}, \code{\link[=canopy3d]{canopy3d()}}
}
