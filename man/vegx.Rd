% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{vegx}
\alias{vegx}
\title{Leaf angle distribution parameter (Lizard, Cornwall)}
\format{
A wrapped \code{SpatRaster} (\code{terra::PackedSpatRaster}), single
layer, 100 m resolution. Unwrap with \code{terra::rast(vegx)} before use.
}
\source{
Derived from \link{landcover} via a per-class leaf angle
distribution lookup table.
}
\usage{
vegx
}
\description{
Example leaf angle distribution raster for the Lizard peninsula,
Cornwall, UK, for use with the \code{x} argument of \code{\link[=solarindex]{solarindex()}},
\code{\link[=skyview]{skyview()}}, \code{\link[=twostream]{twostream()}} and \code{\link[=canopy3d]{canopy3d()}}. This describes how the
leaves in each point's canopy are typically angled, which affects how
much sunlight passing through gets blocked versus slipping through gaps:
values close to 0 mean canopies of mostly upright/vertical leaves (e.g.
many grasses and some conifers), 1 means a "typical" mixed canopy with
leaves at all angles, and large values (up to \code{Inf}) mean canopies of
mostly flat, horizontal leaves (e.g. many broadleaf trees). (Technically,
the ratio of leaf area projected onto a horizontal plane versus a
vertical one, following the Campbell & Norman (1998) ellipsoidal
convention.) Same grid (resolution, extent, CRS) as \code{dtm100m}.
}
\details{
Assigned per land-cover class via a lookup table applied to \link{landcover},
this package's own 10 m resolution European Space Agency land cover
dataset (resampled to this same grid).
}
\examples{
\dontrun{
library(terra)
vegx_r <- rast(vegx)
plot(vegx_r)
}
}
\seealso{
\link{pai}, \link{hgt}, \link{landcover}, \code{\link[=solarindex]{solarindex()}}, \code{\link[=skyview]{skyview()}}, \code{\link[=twostream]{twostream()}}, \code{\link[=canopy3d]{canopy3d()}}
}
\keyword{datasets}
